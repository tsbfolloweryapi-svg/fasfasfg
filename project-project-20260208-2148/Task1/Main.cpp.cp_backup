#include "pch.h"
#include "App.h"



// Точка входа: создает экземпляр App и запускает выполнение
int main() {
	try {
		App app;
		app.run();
	}
	catch (const exception &ex) {
		cout << "Ошибка: " << ex.what() << "\n";
		return 1;
	}

	return 0;
}
